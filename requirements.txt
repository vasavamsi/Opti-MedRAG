# Verified working set (Python 3.11, macOS Apple Silicon).
# Install:  uv venv --python python3.11 .venv && source .venv/bin/activate && uv pip install -r requirements.txt
openai==1.14.2
httpx==0.27.2          # openai 1.14.2 breaks with httpx>=0.28 (removed `proxies`)
tqdm==4.66.1
requests==2.31.0
prettytable==3.9.0
termcolor==2.4.0
pptree==3.1
python-liquid==1.10.2
tiktoken==0.6.0
faiss-cpu
torch
transformers==4.44.2
sentence_transformers==2.2.2
huggingface_hub<0.26   # sentence_transformers 2.2.2 uses the removed `cached_download`
numpy
chardet
google-generativeai

# Optional — only needed for the BM25 retriever (requires a Java runtime):
# pyserini==0.22.1
